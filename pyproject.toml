[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["frontend", ".venv", "venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]  # FastAPI's Depends(...) in defaults is the intended pattern

[tool.ruff.lint.isort]
known-first-party = ["backend", "scripts"]
